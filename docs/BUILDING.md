To build the docs locally you need Python 3.14 installed (some older versions probably work too).

First run this to install the dependencies:
```commandline
pip install -r requirements.txt
```
Now every time you need to build the site, run:
```commandline
make html
```
